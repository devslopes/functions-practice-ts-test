/**
 * FUNCTIONS E15: Additional practice. Importing a type.
 * =====================================================================================
 * @task
 * import the type declared in the './e14.ts'
 * Create a function that works as follows:
 * * The function name is 'isOneOfThemOrTheirSumForty' strictly.
 * * The function takes an argument of an object of imported type
 * * The function returns a boolean:
 * * * true => IF one of the number is 40 OR if their sum is 40
 * * * false => otherwise
 * * Argument type must be annotated
 * * You must use argument destructuring
 * * You must use the imported type Type for argument type annotation
 * * You can use any type of function syntax!
 * Example: isOneOfThemOrTheirSumForty({ num1: 40, num2: 13 }) => true
 * Example: isOneOfThemOrTheirSumForty({ num1: 22, num2: 18 }) => true
 * Example: isOneOfThemOrTheirSumForty({ num1: 23, num2: 16 }) => false
 * !!! The function must be exported !!!
 */

// Your code goes here...

// === TEST YOURSELF ===
// Once you're finished run the test with "npm run test-15"
// If the test has all tests passed, switch to the next exercise file
// If any of the tests fails, refactor the code and run the test command after you've fixed the function
